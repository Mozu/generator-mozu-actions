{
  "name": "<%= name %>",
  "version": "<%= version %>",
  "description": "<%= description %>",
  <% if (repository) { %>
  "repository": "<%= repository %>",
  <% } %>
  "dependencies": {},
  "devDependencies": {}
}
