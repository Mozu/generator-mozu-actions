{
  "name": "<%= name %>",
  "version": "<%= version %>",
  "description": "<%= description %>",
  <% if (repository) { %>
  "repository": "<%= repository %>",
  <% } %>
  "dependencies": {},
  "devDependencies": {
  	"grunt-mozu-appdev-sync": "git+http://tfs.corp.volusion.com:8080/tfs/VNext/Prototype/_git/grunt-mozu-appdev-sync"
  }
}
