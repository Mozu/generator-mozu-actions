{
  "name": "<%= name %>",
  "version": "<%= version %>",
  "description": "<%= description %>",
  <% if (testFramework) { %>
  "scripts": {
    "test": "grunt test"
  },
  <% } %>
  <% if (repository) { %>
  "repository": "<%= repository %>",
  <% } %>
  "dependencies": {},
  "devDependencies": {}
}
